{
  "buildCommand": "cd ../.. && NODE_ENV=development pnpm install --frozen-lockfile=false --prod=false && pnpm build --filter=@hidden-nepal/web",
  "installCommand": "NODE_ENV=development pnpm install --frozen-lockfile=false --prod=false",
  "framework": "nextjs"
}
